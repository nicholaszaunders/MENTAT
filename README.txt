To use MENTAT in a Mathematica notebook, simply run the following line at the beginning of the document:

	<< "path\\MENTAT.wl"

where path is the directory location of the folder in which the MENTAT.wl file is stored, e.g. C:\\Users\\user\\..., etc. The package has been imported successfully when running the above command shows the welcome message.